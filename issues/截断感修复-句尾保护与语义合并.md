任务：减少“话没说完就切”的截断感，保持Chrono节奏

改动摘要：
- UI 接入 ClipGenerator 正式管线（simple_ui.VideoProcessor.generate_video 静态方法）
- 合并参数：gap=1.35 / maxMerged=16.0（维持），新增 min_non_punct_gap_merge=0.35
- 句尾保护：tail_safety_pad 0.45s；若有 original_end 则优先延至 original_end+0.15s（不越过下一段起点-0.02）
- 语义合并：上一段无句末标点 -> gap 阈值乘以 punctuation_merge_factor=1.50，且不低于 min_non_punct_gap_merge
- 音频：片段级淡入淡出（60ms/120ms），降低硬切突兀
- 日志：输出 [Merge] before/after 与参数，便于对比

涉及文件：
- src/ui/simple_ui.py：新增 VideoProcessor.generate_video() 静态方法
- src/core/clip_generator.py：传递 min_non_punct_gap_merge；增强末尾缓冲；可选音频淡入淡出
- configs/clip_settings.json：更新 processing/audio_processing 参数

验证建议：
1) 在UI运行同一工程；日志应出现 [Merge] before=.. after=.. 与 tail/punct 参数；
2) 主观观感：明显减少“话未完即切”，音频过渡更顺滑；
3) 若仍有截断感：
   - tail_safety_pad: 0.45 -> 0.55
   - min_non_punct_gap_merge: 0.35 -> 0.45
   - punctuation_merge_factor: 1.50 -> 1.60

回滚/调整：
- 所有阈值均在 configs/clip_settings.json 可调；
- UI 仍可导出剪映工程作细修；

