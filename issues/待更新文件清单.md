# 待更新文件清单

## 概述
模型升级到Qwen3和Mistral系列后,以下文件仍引用旧的模型名称,需要手动更新。

---

## 需要更新的文件

### 1. 配置文件

#### ✅ 已完成
- `configs/model_config.yaml` - 已更新为新模型
- `configs/models/available_models/qwen3-*.yaml` - 已创建
- `configs/models/available_models/mistral-*.yaml` - 已更新

#### ⚠️ 待处理
- `configs/models/available_models/qwen2.5-7b-zh.yaml` - **建议删除**（旧版本）
- `models/qwen/model_meta.yaml` - 需要更新为Qwen3系列
- `configs/model_config.json` - 需要检查是否使用

---

### 2. 核心模块

#### ⚠️ 待更新
- `src/models/model_manager.py` (第42-65行)
  - 当前硬编码了`qwen2.5-7b-zh`和`mistral-7b-en`
  - **建议**：改为从`configs/model_config.yaml`动态加载

- `src/models/qwen.py` (第35行)
  - 默认模型名称：`qwen2.5-7b-zh`
  - **建议**：改为`qwen3-0.6b`或从配置读取

- `src/models/mistral.py` (第35行)
  - 默认模型名称：`mistral-7b-en`
  - **建议**：保持不变（Mistral-7B仍然存在）

- `src/core/enhanced_model_downloader.py` (第475行)
  - 硬编码了`qwen2.5-7b`下载配置
  - **建议**：改为支持Qwen3系列

- `configs/model_config.py` (第150行)
  - 默认中文模型：`qwen2.5-7b-zh`
  - **建议**：改为`qwen3-0.6b-zh`

---

### 3. 智能推荐系统

#### ✅ 已完成
- `src/core/quantization_analysis.py` - 已添加新模型变体
- `src/core/intelligent_model_selector.py` - 已支持新模型

---

## 更新建议

### 方案1：保守更新（推荐）
1. **保留旧模型兼容性**
   - 不删除`qwen2.5-7b-zh.yaml`
   - 在`quantization_analysis.py`中保留旧模型变体
   - 允许用户选择使用旧模型或新模型

2. **更新默认值**
   - 将默认模型改为Qwen3系列
   - 在UI中提供"使用旧模型"选项

### 方案2：激进更新
1. **完全移除旧模型**
   - 删除所有`qwen2.5-7b`相关配置
   - 强制使用Qwen3系列
   - 更新所有硬编码的模型名称

2. **优点**：代码更简洁
3. **缺点**：可能破坏现有用户的工作流

---

## 优先级

### 🔴 高优先级（必须更新）
1. `src/models/model_manager.py` - 影响模型加载
2. `src/core/enhanced_model_downloader.py` - 影响模型下载

### 🟡 中优先级（建议更新）
3. `src/models/qwen.py` - 默认模型名称
4. `configs/model_config.py` - 默认配置

### 🟢 低优先级（可选）
5. `models/qwen/model_meta.yaml` - 元数据文件
6. `configs/model_config.json` - 可能未使用

---

## 下一步行动

### 建议执行顺序
1. **确认更新策略**：保守 vs 激进
2. **更新model_manager.py**：改为动态加载配置
3. **更新enhanced_model_downloader.py**：支持Qwen3下载
4. **测试验证**：确保所有功能正常
5. **清理旧文件**：删除不再使用的配置

---

## 注意事项

⚠️ **重要提醒**
- 更新前请备份现有配置
- 测试所有模型加载和下载功能
- 确保UI界面正常显示新模型
- 检查训练功能是否受影响

---

## 当前状态

✅ **已完成**
- 智能推荐系统重构
- 新模型配置文件创建
- 自动模型选择逻辑

⏳ **进行中**
- 核心模块更新

❌ **未开始**
- 旧文件清理
- 完整测试验证

