任务：将默认模式切换为“按原时间线去冗快剪”，并提升默认保留比例到 0.70；为运行时加入关键日志，便于UI端确认是否生效。

变更要点
- 引擎默认：CHRONO_MODE=1（启用时序快剪），MONTAGE_MODE=0（关闭蒙太奇）
- 引擎默认：CHRONO_RETAIN=0.70（每集保留比例）
- 日志插桩：在模式选择点打印“剪辑模式选择: CHRONO_MODE=..., MONTAGE_MODE=..., CHRONO_RETAIN=...”；在 _arrange_chronological_fast 内打印“[Chrono] 目标每集保留比例: ...”
- 工具同步：tools/srt_chrono_from_dir.py 默认 retain=0.7

受影响文件
- src/core/real_ai_engine.py
- tools/srt_chrono_from_dir.py

验证建议
- 重启GUI后进行一次导出，查看控制台或日志中是否出现：
  - “剪辑模式选择: CHRONO_MODE=True, MONTAGE_MODE=False, CHRONO_RETAIN=0.7”
  - “启用按原时间线快节奏精剪（去冗、保时间顺序）”
  - “[Chrono] 目标每集保留比例: 0.70”

后续计划（可选）
- GUI 显式开关：提供“时序快剪（默认）/蒙太奇重排”选项，允许调节 CHRONO_RETAIN（0.3–0.9）
- 导出校验：在导出完成后自动运行 srt_sequence_from_meta + srt_content_compare 产出一页可读报告

