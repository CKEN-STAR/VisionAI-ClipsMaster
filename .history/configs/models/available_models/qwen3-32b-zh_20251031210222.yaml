model:
  name: qwen3-32b-zh
  display_name: Qwen3-32B 中文剧本重构模型
  language: zh
  version: '3.0'
  model_format: huggingface
  hf_model_id: Qwen/Qwen3-32B-Instruct
  modelscope_id: qwen/Qwen3-32B-Instruct
  parameters: 32B
  size_fp16: 64.0GB
paths:
  base: models/qwen/qwen3-32b
  quantized: models/qwen/qwen3-32b/quantized
  adapters: models/qwen/qwen3-32b/adapters
  checkpoint: models/qwen/qwen3-32b/base/model.safetensors
quantization:
  default_level: FP16
  emergency_level: FP16
  performance_level: FP16
lora_config:
  flagship_tier:
    r: 64
    lora_alpha: 256
    target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
    lora_dropout: 0.05
    bias: none
    task_type: CAUSAL_LM
training:
  flagship_tier:
    learning_rate: 1e-4
    batch_size: 1
    gradient_accumulation_steps: 16
    num_epochs: 3
    max_seq_length: 16384
    warmup_steps: 500
    logging_steps: 10
    save_steps: 100
    eval_steps: 100
    fp16: true
    bf16: false
    gradient_checkpointing: true
hardware_requirements:
  flagship_tier:
    min_vram: 48GB
    recommended_vram: 80GB
    device_type: GPU
    quantization: FP16
    estimated_inference_time: 1.5s
download:
  modelscope_url: https://www.modelscope.cn/models/Qwen/Qwen3-32B-Instruct
  hf_mirror_url: https://hf-mirror.com/Qwen/Qwen3-32B-Instruct
  hf_url: https://huggingface.co/Qwen/Qwen3-32B-Instruct
  quantized_versions:
    fp16: https://www.modelscope.cn/models/Qwen/Qwen3-32B-Instruct
  resume_download: true
  max_retries: 3
  timeout: 1200
剧本重构配置:
  max_input_length: 32000
  max_output_length: 8000
  temperature: 0.7
  top_p: 0.9
  top_k: 50
  repetition_penalty: 1.1
  min_output_ratio: 0.2
  max_output_ratio: 0.8
  system_prompt: '你是一个专业的短剧剪辑师，擅长从原片字幕中提取精彩片段，重新组织成吸引人的混剪视频字幕。

    你的任务是：

    1. 理解原片的剧情走向和情感曲线

    2. 提取最精彩、最吸引人的片段

    3. 重新组织这些片段，确保剧情连贯、情感递进

    4. 生成新的字幕文件，包含时间码和对话内容


    注意事项：

    - 保持剧情连贯性，避免跳跃过大

    - 确保情感曲线合理，有起承转合

    - 避免过短（剧情不完整）或过长（与原片相差不大）

    - 优先选择冲突、转折、高潮等关键情节

    '
  user_prompt_template: '原片字幕如下：

    {original_srt}


    请根据以上原片字幕，生成一个吸引人的混剪视频字幕。要求：

    1. 提取最精彩的片段

    2. 确保剧情连贯

    3. 生成新的SRT格式字幕文件

    4. 字幕长度控制在原片的{min_ratio}到{max_ratio}之间

    '
benchmark:
  device: GPU (NVIDIA A100 80GB)
  quantization: FP16
  inference_time_per_subtitle: 1.5s
  memory_usage: 70.0GB
  bleu_score: 0.92
known_issues:
- 需要至少48GB显存用于FP16训练
- 推荐使用80GB显存的GPU（A100）
- 适合专业级工作站
changelog:
- version: '1.0'
  date: '2025-01-31'
  changes:
  - 初始版本（Qwen3系列）
  - 支持旗舰级设备
  - 支持LoRA微调
  - 最强的指令遵循能力
  - 最强的中文理解能力
  - 最好的输出格式控制
  - 最强的推理能力

