# VisionAI-ClipsMaster 完整依赖清单
# 基于智能推荐下载器系统需求生成 (v1.2.0)
# 更新时间：2025-10-11
# 注意：版本号已根据实际安装版本更新，确保依赖兼容性

# ============================================
# UI界面相关依赖
# ============================================
PyQt6==6.9.1  # 实际安装版本
plotly==6.3.0  # 实际安装版本
matplotlib==3.10.7  # 实际安装版本

# ============================================
# AI/ML核心依赖（升级版本以支持Qwen3和Mistral）
# ============================================
torch==2.9.0+cu128  # 实际安装版本（CUDA 12.8）
transformers==4.48.3  # 实际安装版本，支持Qwen3
numpy==2.2.6  # 实际安装版本
scikit-learn==1.7.2  # 实际安装版本

# ============================================
# 设备信息采集
# ============================================
nvidia-ml-py>=12.0.0  # GPU信息检测（推荐，替代已弃用的pynvml）
py-cpuinfo==9.0.0  # 实际安装版本
# psutil>=5.9.0  # 已在系统监控依赖中

# ============================================
# 模型管理与下载
# ============================================
modelscope>=1.9.0  # 国内镜像下载
huggingface-hub==0.35.3  # 实际安装版本

# ============================================
# 量化工具（可选，用于降低模型内存需求）
# ============================================
# auto-gptq>=0.4.0  # GPTQ量化（已弃用，不再支持）
bitsandbytes>=0.41.0  # LLM.int8()量化

# ============================================
# GGUF推理引擎（用于真实AI推理）
# ============================================
llama-cpp-python>=0.2.0  # GGUF格式模型推理引擎

# ============================================
# 微调工具
# ============================================
peft==0.17.1  # 实际安装版本（LoRA/QLoRA）
accelerate==1.10.1  # 实际安装版本（分布式训练和模型加载）

# ============================================
# 视频处理依赖
# ============================================
opencv-python==4.12.0.88  # 实际安装版本
ffmpeg-python==0.2.0  # 实际安装版本

# ============================================
# 文本处理依赖（中英文NLP）
# ============================================
jieba==0.42.1  # 实际安装版本
spacy==3.8.7  # 实际安装版本

# ============================================
# 系统监控依赖
# ============================================
psutil==7.0.0  # 实际安装版本
GPUtil==1.4.0  # 实际安装版本
WMI==1.5.1  # 实际安装版本

# ============================================
# 网络和异步支持
# ============================================
aiohttp==3.12.15  # 实际安装版本
requests==2.32.5  # 实际安装版本

# ============================================
# 开发和测试依赖
# ============================================
pytest==8.4.1  # 实际安装版本
pytest-qt==4.5.0  # 实际安装版本

# ============================================
# 其他核心依赖
# ============================================
Pillow==11.3.0  # 实际安装版本
PyYAML==6.0.3  # 实际安装版本
tqdm==4.67.1  # 实际安装版本

# ============================================
# 可选依赖（高级功能）
# ============================================
# tensorboard>=2.10.0  # 训练监控（可选）
# jupyter>=1.0.0       # 开发环境（可选）
# llama-factory>=0.1.0  # 高级训练框架（可选，提供更多训练功能）
# unsloth>=2023.11.0    # 训练加速（可选，加速LoRA/QLoRA训练）

# ============================================
# 依赖说明
# ============================================
# 1. transformers>=4.51.0 是必需的，以支持Qwen3模型
# 2. bitsandbytes 用于模型量化，降低内存需求（auto-gptq已弃用）
# 3. peft 用于LoRA/QLoRA微调，在量化模型上训练适配器
# 4. modelscope 提供国内镜像下载，加速模型下载
# 5. pynvml 和 py-cpuinfo 用于设备信息检测，实现智能推荐


# ============================================
# Web/API 依赖
# ============================================
fastapi>=0.115,<0.200
pydantic>=2,<3
uvicorn[standard]>=0.22.0
PyJWT>=2.8.0
python-multipart>=0.0.9

# ============================================
# 测试依赖（仅用于本地/CI）
# ============================================
httpx>=0.27.0
