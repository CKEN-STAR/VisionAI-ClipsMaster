# 分支保护 - 简化版
# 暂时禁用，待项目测试体系完善后启用

name: Branch Protection

on:
  workflow_dispatch:  # 仅手动触发

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - name: Placeholder
      run: echo "Branch Protection workflow - 待完善"
