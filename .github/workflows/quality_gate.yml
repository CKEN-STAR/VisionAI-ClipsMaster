# 质量门禁 - 简化版
# 暂时禁用，待项目测试体系完善后启用

name: Quality Gate

on:
  workflow_dispatch:  # 仅手动触发

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - name: Placeholder
      run: echo "Quality Gate workflow - 待完善"
